# Throwaway device/debug DB snapshots pulled via ADB — not source
*.db

# Local agent/IDE settings
.claude/settings.local.json

# Scratch test logs
testlog*.txt

# API kljucevi — drzati izvan javnog repoa (vidi dart_defines.example.json)
dart_defines.json
**/dart_defines.json
